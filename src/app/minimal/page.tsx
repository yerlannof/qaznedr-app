export default function MinimalPage() {
  return (
    <div>
      <h2>Minimal Page</h2>
      <p>This is a minimal page with no providers or complex components.</p>
      <p>If this loads, the basic setup is working.</p>
    </div>
  );
}